### Input file ###
Just add in this file `ips.csv` your own list of IPs

### Output file ###
When script is done it will create `ip_countries.csv`

### Running the script ###
You just need to run `python3 ip_geolocate.py`

the script uses (IP-API.COM){http://ip-api.com} API
